# Forca Webapp Backend
Backend da aplicação web-app de forca, utilizando python com flask

# Steps to run

* Install pip

* python -m venv <env-name> (If there's no venv on root, you neet to create one virtual env)

* Activate de venv

* execute run_server.bat